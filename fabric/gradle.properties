# MC
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.20.2)
java_version=17

# Fabric (aligned with ControlFlex dev/1.20.1)
fabric_loader_version=0.15.11
fabric_api_version=0.92.2+1.20.1

# Create Fabric (Fabricators-of-Create fork), compile-only target for the shared mixin.
# Fetched as a plain file (see fabric/build.gradle) to avoid Loom 1.10 dependency
# validation; the fork's mod id is "create" and its
# ControlsUtil.isActuallyPressed(KeyMapping) matches the upstream signature.
create_fabric_version=6.0.8.1+build.1744-mc1.20.1
create_fabric_url=https://cdn.modrinth.com/data/Xbc0uyRg/versions/HAqwA6X1/create-fabric-6.0.8.1%2Bbuild.1744-mc1.20.1.jar

# Runtime dependency mod versions
# Kept in step with the Forge side: the API floor is 0.8.5, so a 0.8.7 runtime
# requirement leaves room for 0.8.8 pre-release builds.
controlflex_version_range=>=0.8.7
create_version_range=>=6.0.7
